# fs:		psmisc-23.2
# sauce:	https://sourceforge.net/projects/psmisc/files/psmisc/psmisc-23.2.tar.xz

# 1
./configure --prefix=/usr

# 2
make && make install

# 3
mv -v /usr/bin/fuser   /bin
mv -v /usr/bin/killall /bin
